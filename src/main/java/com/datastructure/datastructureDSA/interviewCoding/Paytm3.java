/*
package com.datastructure.datastructureDSA.interviewToday;

import com.datastructure.datastructureDSA.java.record.Person;

import java.util.Comparator;
import java.util.stream.Collectors;

public class Paytm3 {
    public static void main(String[] args) {
        List<Person> persons = Arrays.asList(
                new Person("Alice", "New York"),
                new Person("Bob", "Los Angeles"),
                new Person("Charlie", "New York"),
                new Person("David", "Los Angeles"),
                new Person("Eve", "Chicago")
        );

        List<Person> persons = Arrays.asList(
                new Person("Alice", 30),
                new Person("Bob", 25),
                new Person("Charlie", 35)
        );


        Map<String, Long> peoples = person.stream()
                .collect(Collectors.groupingBy(Person::getCity),Collectors.counting());

        peoples.forEach(city , count ) ->
        System.out.println()


          Optional<Person> yougest = persons.stream().min(Comparator.comparingInt(Person::getAge));

        yougest

        Employee - name, age , salary

        select distinct salary
        from Employee
        order by salary desc
        limit 1 offset n-1;

    }
}
*/
